<UserControl x:Class="Paygl.Views.EditFiltersGroup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Paygl.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Style="{StaticResource MyGrid}">
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>
        <TextBox Name="_lblName" HorizontalAlignment="Stretch" VerticalAlignment="Center" TextWrapping="Wrap" Text="" Grid.Row="0" Style="{StaticResource MyTextBox}"/>
        <Border Style="{StaticResource MyBorderMedium}" Margin="10,10,10,10" Grid.Row="1">
            <ScrollViewer HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
                <StackPanel Name="_spDisplay" Orientation="Vertical">

                </StackPanel>
            </ScrollViewer>
        </Border>
        <Button Name="_btnSave" Style="{StaticResource MyButton}" Content="Zapisz" Grid.Row="2" Click="_btnSave_Click"></Button>
    </Grid>
</UserControl>
